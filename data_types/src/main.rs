fn main() {
    // Integers
        // i32 = positive or negative
        // u32 = only positive
        // 8, 16, 32, 64, 128, arch are available
    let a = 98_222; // Decimal
    let b = 0xff; // Hex
    let c = 0o77; // Octal
    let d = 0b1111_0000; // Binary
    let e = b'A'; // Byte (u8 only)

    // Floating-point numbers

    let f = 2.0;
    let g: f32 = 3.0;

        // addition
        let sum = 5 + 10;
        // subtraction
        let difference = 95.5 - 4.3;
        // multiplication
        let product = 4 * 30;
        // division
        let quotient = 56.7 / 32.2;
        // modulus
        let remainder = 43 % 5;

    // Booleans

    let t = true;
    let f: bool = false;

    // Character

    let c = 'z';
    let z = 'Z';
    let heart_eyed_cat = '😻';
}
